// import { expose } from "threads/worker"
// import { iFolder } from "../../../../shared/types.shared"
// import { getFolderHierarchySync } from "../dir.manager"

// expose({
//     workerFunc: async (name:string):Promise<iFolder> => {
//         return await getFolderHierarchySync('../../data')
//         // return `wooo im from the worker and your name ${name} is a fun one`
//     }    
// })
 
export const nothing = {}